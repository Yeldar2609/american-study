# Debug Journal - Vitest worker startup
Started: 2026-06-19T13:00:00+05:00
Goal: Restore reliable test execution for the admin analytics release.

## Environment snapshot (Phase 0)
- Runtime: Node.js, npm launcher, Vitest
- Entry: `npm.cmd test -- tests/unit/admin-analytics-data.test.ts`
- Git HEAD: `beedfbb`; working tree intentionally dirty
- References read: Node runtime, setup, investigation

## Hypotheses
1. [CONFIRMED] OneDrive path canonicalization breaks Vitest module loading. Evidence: the same 11 tests pass through a junction when Vite `preserveSymlinks` is enabled.
2. [REFUTED] The installed Vitest/jsdom dependency set is incompatible with the active Node runtime. The same installed versions pass unchanged through the corrected path.
3. [REFUTED] Stale Vite/Vitest caches are the primary cause. No cache deletion was needed for the green run.

## Failed hypothesis round counter
- Round 1: pending

## Artifacts to revert
- [x] `C:\codex-work\american-study` junction. Removed after target verification.
- [ ] `C:\codex-work\vitest-junction.config.mts`. Remove after the junction-safe probe.
- [ ] Two temporary Supabase Auth QA users for admin/non-admin RPC verification. Delete public profiles and Auth users in the command's `finally` block.
- [ ] `.verify-admin-analytics.mjs`. Remove after remote RPC verification.

## Findings
- 2026-06-19: Through the junction, Vitest workers started and reached suite loading, then failed because `setupFiles` resolved to `/@fs/C:/Users/yelda/OneDrive/Desktop/American Study App/tests/setup.ts`. This refutes a generic worker-start failure and implicates path canonicalization/config resolution.
- 2026-06-19: With `resolve.preserveSymlinks=true` in the disposable junction config, `admin-analytics-data.test.ts` passed 11/11 on Node 22.18.0, Vitest 4.1.9, Vite 8.0.16, and jsdom 29.1.1.

## Final fix
